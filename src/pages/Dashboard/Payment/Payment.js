import Typography from '@mui/material/Typography';
import React from 'react';

const Payment = () => {
    return (
        <div>
             <Typography variant="h3" sx={{mt: 10,color: '#0277bd'}} gutterBottom>
                 Payment option is coming soon.
             </Typography>
             <Typography variant="h4" sx={{color: '#d50000'}} gutterBottom>
                  wait wait wait.
             </Typography>
        </div>
    );
};

export default Payment;